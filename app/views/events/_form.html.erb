<div>
<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time, style: "width: 100% !important;padding: 8px 10px 7px;" %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time, style: "width: 100% !important;padding: 8px 10px 7px;" %>
  </div>
  <div>
  	<%= f.label :company %><br>
  	<%= f.collection_select :company_id, @companies, :id, :username %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
